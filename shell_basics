#To find the biggest files in your system and remove them
find . -printf '%s %p\n'| sort -nr | head -10
#To print the size of the file in MB or GB
ls -lah filename.txt
#use with caution, recursive and irrevocable delete
rm -rf filename.txt
